// this code will no longer work on frontend for InstantAccount. Just fetch the data from backend instead.
// import getDayMonthBr from "../../../../utils/dates/getDayMonthBr";
// import getDateCode from "../../../../utils/dates/getDateCode";

// export default function generateBirthCode(birthDate) {
//     if(!birthDate) return;

//     const opts = { needYear: true };
//     const thisBirthDate = getDayMonthBr(birthDate, opts);
//     const { code: birthCode } = getDateCode(thisBirthDate);

//     return birthCode;
// }
