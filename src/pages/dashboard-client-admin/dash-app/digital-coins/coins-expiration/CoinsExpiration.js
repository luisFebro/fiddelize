export default function CoinsExpiration() {
    return (
        <section>
            <h2 className="text-subtitle text-purple">
                I am the CoinsExpiration
            </h2>
        </section>
    );
}
